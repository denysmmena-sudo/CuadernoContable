name: Compilar APK con Flet

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Clonar repositorio
      uses: actions/checkout@v4

    - name: 🐍 Configurar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: ☕ Configurar Java (Requerido para Android)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 📦 Instalar dependencias de Python
      run: |
        python -m pip install --upgrade pip
        pip install flet pandas openpyxl reportlab

    - name: 🛠️ Construir APK con Flet
      run: |
        flet build apk --project-name "ComercialPablitto" --description "Registro Diario de Ventas"

    - name: 💾 Subir APK como artefacto
      uses: actions/upload-artifact@v4
      with:
        name: app-release-apk
        path: build/apk/app-release.apk
